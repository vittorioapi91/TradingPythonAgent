# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
env/
ENV/
.venv
.venv-kserve
# Jenkins venv (created during CI/CD)
.venv-jenkins/

# IDE
# Allow all .vscode configuration files (especially launch.json files)
.vscode/*
# Explicitly allow all JSON files (including all launch.json configurations)
!.vscode/**/*.json
# Allow markdown and Python files in .vscode
!.vscode/**/*.md
!.vscode/**/*.py
# Ensure all launch.json files are tracked (explicit patterns for clarity)
!.vscode/launch.json
!.vscode/**/launch.json
.idea/
*.swp
*.swo
*~

# Environment variables
.env
.env.local
.env.dev
.env.staging
.env.prod

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Jenkins data (runtime/operational data - should not be versioned)
# Exclude entire data directory - all runtime Jenkins data
.ops/.jenkins/data/

# Large data files
*.parquet
**/backup_parquet/
**/edgar_companies/
src/trading_agent/fundamentals/edgar/master/
company_tickers.json

# Python build artifacts (keep directory, ignore contents)
build/
dist/
*.egg-info/

# Airflow wheels (wheels are built and copied here, but not versioned)
.ops/.airflow/wheels/*.whl

# Airflow log directories
.ops/.airflow/logs-*/
.ops/.airflow/logs-dev/
.ops/.airflow/logs-test/
.ops/.airflow/logs-prod/

# SSL certificates (self-signed for local development)
.ops/.docker/ssl/*.key
.ops/.docker/ssl/*.crt
